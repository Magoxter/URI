    /* Ultrapassing Z */
#include <stdio.h>

int main (void) {
  int k,y,x,
      sum;
   scanf("%d", &x);
   scanf("%d", &y);
 while (y <= x)
  scanf("%d", &y);
  k = 0;
 while (sum < y) {
   sum += x++;
  k++;
 } printf("%d\n", k);
 return 0;
}
